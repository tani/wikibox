sudo: false
language: node_js
node_js:
  - 10
cache:
  directories:
    - "$HOME/.npm"
script:
  - make
deploy:
  provider: pages
  local-dir: ./docs/
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
